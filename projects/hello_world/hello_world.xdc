# Project-specific pin constraints for hello_world.
# Board-level fixed nets (clock, LEDs) live in boards/zcu104/xdc/ and are
# pulled in automatically by scripts/build.tcl — nothing project-specific
# is needed here since this design only uses the clock and the LEDs.
